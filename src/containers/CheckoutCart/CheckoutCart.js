import React from 'react';
import { Container, CheckoutButton } from './CheckoutCart.style';

const CheckoutCart = () => {
  return (
    <Container>
      <CheckoutButton>Checkout</CheckoutButton>
    </Container>
  );
};

export default CheckoutCart;
